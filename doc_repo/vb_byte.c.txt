#include int main (){  unsigned int testdata[] = {12, 222, 654, 36000, 1000000};  const int num_testdata = sizeof(testdata)/4;  unsigned int vbytes[sizeof(testdata)];  int num_vbytes = 0;  unsigned int carry, onenum, onebyte, movebyte, nbits;  int i;  for ( i=0; i 0 )     {	carry = 0;        	if ( onenum < 128 ) carry = 128;         printf ("%2.2X ", carry + (onenum % 128) );        vbytes [num_vbytes++] = carry + (onenum % 128 );	onenum = onenum >> 7;   /* shift right 7 bits, i.e. divide by 128 */     }  }  printf ("\ndone encoding the variable bytes, now decode them \n");  onenum = 0;  nbits = 0;  for ( i=0; i < num_vbytes; i++ )  {     onebyte = vbytes[i];       printf ("%2.2X ", onebyte);      movebyte = (onebyte%128) << nbits; nbits += 7;     onenum = onenum + movebyte;      if ( onebyte > 127 ) /* if carry set, end of number */     {        printf (" decoded as %d\n", onenum );        onenum = 0;        nbits = 0;     }   }   printf ("\ndone\n");   return 0;}